node_modules/
npm-debug.log*
*.log

# Local environment overrides
.env
.env.local

# OS files
.DS_Store
Thumbs.db

# Generated report output (regenerated on every run). Ignored so local runs
# don't create noisy diffs. In CI the report is published straight to GitHub
# Pages from this folder (never committed); only data/state.json is committed.
public/*.html
public/*.json
